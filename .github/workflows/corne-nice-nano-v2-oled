# Copyright 2022 Manna Harbour
# https://github.com/manna-harbour/miryoku

name: 'Build Example corne nice_nano display'
on: workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    secrets: inherit
    with:
      board: '["nice_nano_v2"]'
      shield: '["corne_left","corne_right","settings_reset"]'
      alphas: '["qwerty"]'
      kconfig: '["CONFIG_ZMK_DISPLAY=y","CONFIG_BT_CTLR_TX_PWR_PLUS_8=y"]'
      # Optional: Uncomment and configure additional options below
      # nav: '["default"]'          # Options: default, invertedt, vi
      # layers: '["default"]'       # Options: default, flip
      # mapping: '["default"]'      # Options: default, extended_thumbs
      clipboard: '["win"]'    # Options: default, mac, win
      tap: '["qwerty"]'          # Alternative alphas for Tap layer
      extra: '["qwerty"]'        # Alternative alphas for Extra layer
      # custom_config: '["default"]' # Example: "#define MIRYOKU_CLIPBOARD_WIN"
      # kconfig: '["default"]'      # Example: "CONFIG_ZMK_SLEEP=y"

